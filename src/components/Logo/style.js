import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
    
    boxLogo: {
        marginBottom: 40,
        alignItems: 'center',
    },

    logo: {
        width: 300,
        height: 250,
    },

});

export default styles